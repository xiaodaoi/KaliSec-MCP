# Kali MCP Server 依赖
# Python 3.8+ required

# MCP SDK (Model Context Protocol)
mcp>=1.0.0

# 异步支持
aiohttp>=3.8.0
aiofiles>=23.0.0

# 数据验证
pydantic>=2.0.0

# SSE 模式依赖 (远程访问)
starlette>=0.27.0
uvicorn>=0.23.0
sse-starlette>=1.6.0

# FastMCP 支持
fastmcp>=0.1.0

# HTTP 请求
requests>=2.28.0

# 可选: PWN工具支持
# pwntools>=4.11.0
